pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/bmaity-lg/security-chatbot.git'
            }
        }

        stage('Build') {
            steps {
                // Your build commands here
               // sh 'mvn clean package'
            }
        }

        stage('Test') {
            steps {
                // Your test commands here
               // sh 'mvn test'
            }
        }

        stage('Deploy') {
            steps {
                // Your deployment commands here
               // sh 'kubectl apply -f deployment.yaml'
            }
        }
    }
}
